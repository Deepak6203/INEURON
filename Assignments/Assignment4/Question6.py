# Write a python script to calculate the area of Triangle. Number is entered by the user.


# HALF = 1/2
half = 0.5
base = float(input("Enter Base :"))
height = float(input("Enter Height :"))
 
 #Operation 
 #Fromula area = Half * base * height
print(half*base*height)